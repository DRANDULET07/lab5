# Отчет об использовании ИИ (AI Report)

**Студент:** [Твое Имя]
**Лабораторная работа:** №5 (TypeScript & Props Validation)

## 1. Роль ИИ в выполнении работы
В ходе выполнения данной лабораторной работы искусственный интеллект (Gemini) использовался как интерактивный наставник и инструмент для отладки. Основные задачи, решенные с помощью ИИ:
* **Разъяснение теории**: ИИ помог понять разницу между `Interface` и `Type Alias`, а также объяснил концепцию `Union Types` для уровней навыков (`SkillLevel`).
* **Типизация событий**: ИИ предоставил правильные типы для обработчиков событий React (`React.ChangeEvent<HTMLInputElement>` и `React.MouseEvent<HTMLButtonElement>`).
* **Настройка окружения**: Помощь в устранении ошибок Git («Author identity unknown») и решение проблем с разрешением модулей TypeScript после установки зависимостей.

## 2. Примеры запросов (Prompts)
Для решения задач использовались следующие типы запросов:
* "Как правильно типизировать пропс children в TypeScript для React?"
* "Почему VS Code подчеркивает красным импорт React и как это исправить через терминал?"
* "Помоги составить структуру интерфейсов для пользователя и его навыков согласно методичке."
* "Как сделать коммит через терминал, если Git просит настроить email и name?"

## 3. Анализ и проверка
Весь код, предложенный ИИ, был проанализирован мной на соответствие регламенту OquLabs:
* Я убедился, что не используется тип `any`.
* Я самостоятельно проверил структуру папок `task1` и `task2`.
* Я вручную протестировал работу фильтрации в `SearchApp.tsx`, чтобы убедиться в корректности логики поиска.

## Заключение
Использование ИИ позволило быстрее разобраться с синтаксисом TypeScript и сосредоточиться на логике компонентов, не тратя лишнее время на поиск специфических типов событий в документации.